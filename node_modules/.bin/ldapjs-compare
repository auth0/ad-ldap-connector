#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ldapjs/bin/ldapjs-compare" "$@"
  ret=$?
else 
  node  "$basedir/../ldapjs/bin/ldapjs-compare" "$@"
  ret=$?
fi
exit $ret
