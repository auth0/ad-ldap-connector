#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ldapjs/bin/ldapjs-add" "$@"
  ret=$?
else 
  node  "$basedir/../ldapjs/bin/ldapjs-add" "$@"
  ret=$?
fi
exit $ret
